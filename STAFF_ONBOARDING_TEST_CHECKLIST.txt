1. Go to http://bandhuapp.local:8000/login and log in as Administrator / admin.
2. Open the Admin workspace, click the "Onboard Staff" shortcut -> confirm it opens /desk/staff-onboarding with full Desk chrome (sidebar, search, notifications) and the form loads with Role and Gender dropdowns populated.
3. Submit with First Name blank -> confirm error "First name is required."
4. Submit with Email = priya.doctor@bandhuclinic.test (already used) -> confirm error "A user with email ... already exists."
5. Submit with Email = notanemail -> confirm a validation error, not a raw traceback.
6. Submit with Mobile = 12345 -> confirm error "Mobile number must be 10 digits."
7. Create a new Doctor: fill First Name, Last Name, Email, Mobile, Role=Doctor, Gender -> confirm "Account created." with a user email and HLC-PRAC-... id shown.
8. Repeat step 7 with Role=Nurse.
9. Repeat step 7 with Role=Clinic Assistant cum Driver.
10. Confirm all form fields are empty again after each successful submit (ready for the next entry).
11. Log out of Administrator.
12. Log in as the Doctor account from step 7 using the password in TEST_CREDENTIALS.txt -> confirm you land on /desk/doctor-form with full Desk chrome (sidebar, search, notifications, your name at bottom-left).
13. Log in as the Nurse account from step 8 -> confirm you land on /desk/nurse-form with full Desk chrome.
14. Log in as the CAD account from step 9 -> confirm you land on /desk/cad-form with full Desk chrome.
15. While logged in as the Doctor account, go directly to http://bandhuapp.local:8000/desk/staff-onboarding -> confirm Frappe's native "not permitted" page (this is now enforced by the Page doctype's own roles field, not custom code).
16. While logged in as the Doctor account, go to /desk/nurse-form and then /desk/cad-form -> confirm both are blocked the same way.
17. Use the "Refresh" button in the page header (top-right, next to the breadcrumb) on the Doctor/Nurse/CAD pages -> confirm it reloads the queue without a full page reload.
18. In Desk, open the Healthcare Practitioner list -> confirm all 3 new practitioners exist with the right custom_role, user_id, and status = Active.
19. In Desk, open the User list -> confirm each of the 3 new users has exactly one role (Doctor / Nurse / Clinic Assistant cum Driver), not zero or multiple.
20. In Desk, open the Error Log list -> confirm one entry per new account titled "Staff onboarding: set-password email failed" (expected here since this bench has no Outgoing Email Account configured).
21. Run a full patient through the clinic loop using the 3 new accounts (CAD registers -> Doctor reviews -> Nurse completes) to confirm they work in the real workflow, not just at login. On the Doctor/Nurse queue tables, click a patient row and confirm it opens the Patient Encounter form via Desk's own router (no full page reload), not a raw link.
